@model WebApp.Models.cat_branches

@{
    ViewData["Title"] = "Edit Sucursal";
}

<h1>Editar</h1>

<h4>Sucursal</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div class="form-group">
                <label asp-for="BRA_NAME" class="control-label"></label>
                <input asp-for="BRA_NAME" class="form-control" Autocomplete="off" />
                <span asp-validation-for="BRA_NAME" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BRA_ACTIVE" class="control-label"></label>
                <br />
                <select asp-for="BRA_ACTIVE" class="form-control" asp-items="@ViewBag.StatusOptions"></select>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<br/>
<div>
    <a asp-action="Index">Regresar a la lista</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
