@model WebApp.Models.cat_dealers

@{
    ViewData["Title"] = "Edit User";
}

<h1>Editar</h1>

<h4>Distribuidor</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div class="form-group">
                <label asp-for="DEA_NAME" class="control-label"></label>
                <input asp-for="DEA_NAME" class="form-control" Autocomplete="off" />
                <span asp-validation-for="DEA_NAME" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DEA_ACTIVE" class="control-label"></label>
                <br />
                <select asp-for="DEA_ACTIVE" class="form-control" asp-items="@ViewBag.StatusOptions"></select>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<br/>
<div>
    <a asp-action="Index">Regresar a la lista</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
