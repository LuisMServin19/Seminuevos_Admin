@model IEnumerable<Serfitex.Models.Ta_gastos>

@{
    ViewData["Title"] = "Index";
    var fiperfil = ViewContext.HttpContext.Session.GetString("fiperfil");
}

<h1>Gastos Tenencias</h1>

@if (fiperfil == "1")
{
    <p>
        <!-- Puedes añadir cualquier mensaje o botón aquí si es necesario -->
    </p>
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.First().Concepto) <!-- Asegúrate de que esto corresponda -->
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count(); i++) // Cambiado a Count() para el índice
        {
            var item = Model.ElementAt(i); // Obtiene el item actual del modelo
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Concepto)
                </td>
                
                <td>
                    @if (fiperfil == "1")
                    {
                        <a asp-action="Gasto" asp-route-id="@item.Id_unidad" class="btn btn-primary">Agregar gasto</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
