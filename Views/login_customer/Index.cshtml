@model IEnumerable<WebApp.Models.login_customer>

@{
    ViewData["Title"] = "Index";
}

<h1>Clientes</h1>

<p>
    <a asp-action="Create">Crear nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LOG_NAME)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LOG_ACTIVE)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.LOG_NAME)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID_LOG" class="btn btn-primary">Editar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
